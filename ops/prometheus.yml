global:
  # Short local experiments need more than one sample inside a 5-10 second change.
  scrape_interval: 1s

scrape_configs:
  - job_name: limited-goods-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
