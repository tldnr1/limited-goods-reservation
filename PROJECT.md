# 프로젝트 계약

한정 굿즈의 다중 상품 점유와 결제를 제한된 자원에서 안전하게 처리한다.
현재 구현은 Java로 새로 작성한 실행 가능한 기준선이다. 목표 성능을 달성했다는 뜻은 아니다.
Target v1 첫 구현은 [실행·검증 범위](docs/architecture/target-v1.md)를 따른다. 아래 시간 계약은 공통이며,
대기 입장권 필수 적용과 역할 격리는 `compose.target.yml` 오버레이에서 활성화한다.

## 비즈니스 흐름

판매 시작 전 조회는 가능하다. 시작 후 구매 요청은 모든 상품을 함께 점유하거나 아무것도 변경하지 않는다.
판매·상품 등록 API와 X-User-Id는 로컬 시나리오 준비용이다. 실제 로그인, 판매자 권한, 실제 PG는 범위 밖이다.

- 사용자별·상품별 활성 점유 + 확정 구매 합이 인당 제한을 넘지 않는다.
- 각 상품의 total = available + held + sold이며 모든 수량은 0 이상이다.
- 장바구니를 서버에 저장하지 않는다. 구매 요청의 items가 이번 점유 대상이다.
- 주문은 PAYMENT_PENDING → PAYMENT_PROCESSING → CONFIRMED 경로를 따른다.
- 결제 미시도 주문은 점유 생성부터 300초 후 EXPIRED로 바뀌고 재고를 반환한다.
- 결제 API는 기한 내 요청을 DB에 저장하고 202를 반환한다. 202는 결제 성공이 아니다.
- 결제 실패가 확정되면 원래 300초 기한 안에서 같은 주문에 새 결제 시도를 할 수 있다.
- Mock PG 최초 처리 허용 기한은 각 결제 시도 접수 시각 + 70초다. 같은 시도의 재조회는 이를 연장하지 않는다.
  점유 만료와 독립된 임시 정책이며 토스의 보장값이나 지연 SLO가 아니다.
- PG는 최초 처리 요청이 기한 뒤에 도착하면 실패시키며, 이미 처리한 요청의 재조회는 같은 결과를 돌려준다.
- UNKNOWN 또는 처리 중인 결제의 재고는 기한만으로 반환하지 않는다. 새 결제를 막고 결과를 확인한다.
- 늦게 확인된 성공도 재고를 계속 보유하고 있다면 확정한다. 영구 UNKNOWN의 수동 복구는 후속 범위다.
- 동일 주문의 성공 결제는 하나다. 중복 콜백은 재고를 다시 변경하지 않는다.

구매 멱등키는 사용자 단위, 결제 멱등키는 주문 단위이다.
같은 키에 다른 요청 본문을 보내면 409다. 일시적 429/503 이후에는 같은 키로 재시도할 수 있다.
주문 조회와 결제 시도는 소유자를 확인한다.

## 실험 시나리오와 목표

아래 분포는 기존 baseline harness의 계약이다. Target v1은 첫 5초 30,000명 / 다음 10초 15,000명 /
다음 45초 5,000명, 정상 초기 점유 60초·950개 확정 120초를 후속 검증 목표로 둔다.
이번 첫 구현은 기능 검증 단계이며 이 부하 및 성능 목표 달성을 주장하지 않는다.

재고 1,000개, 관심 사용자 50,000명(50:1)은 프로젝트가 정한 가정이다.
첫 10초 30,000명의 최초 시도(평균 3,000 RPS), 다음 50초 20,000명(400 RPS)을 기본 분포로 둔다.
재시도 트래픽은 이 최초 시도 수치와 분리해서 기록한다. 이 숫자가 실제 업계 평균이라는 주장은 하지 않는다.

정합성 위반 0건, 정상 PG에서 120초 내 950개 판매, 최초 점유 응답 99% 1초 이내,
예기치 않은 오류 0.1% 이하, 결제 접수 p95 1초 이내를 목표로 둔다.
429 비율과 결과별 지연시간을 따로 기록하여 빠른 거절만으로 목표 달성을 주장하지 않는다.
전체 합격 항목과 현재 검증 범위는 [성능 문서](docs/performance.md)를 따른다.

실제 결제·배송·환불·엄격한 FIFO 대기열·Kafka·Kubernetes·AWS 배포·프론트엔드는 현재 범위 밖이다.
